# Copy this file to .env for local reference only.
# Real values must be set in the hosting provider's Environment Variables
# (e.g. Vercel → Project → Settings → Environment Variables), never committed.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
